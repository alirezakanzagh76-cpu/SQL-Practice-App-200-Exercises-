# SQL-Practice-App-200-Exercises-
An offline, beginner-friendly SQL practice desktop app built with **Electron + sql.js (SQLite in WebAssembly)**.   It includes **200 SQL exercises**, a built-in editor, instant results, and a safe execution mode (read-only queries).
